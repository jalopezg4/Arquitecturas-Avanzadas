name: ms-documentos CI

on:
  push:
    paths:
      - "services/ms-documentos/**"
      - ".github/workflows/ms-documentos.yml"
  pull_request:
    paths:
      - "services/ms-documentos/**"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/ms-documentos
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: services/ms-documentos/package-lock.json
      - run: npm ci
      - run: npm run scan:secrets
      - run: npm test

  build:
    needs: test
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/ms-documentos
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t ms-documentos:${{ github.sha }} .
